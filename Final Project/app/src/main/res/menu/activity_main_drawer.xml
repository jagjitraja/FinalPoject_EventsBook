<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/events_line"
            android:icon="@drawable/ic_dashboard_black_24dp"
            android:title="Events Line" />
    </group>

    <item android:title="My Profile &amp; History">
        <menu>
            <item
                android:id="@+id/posted_events"
                android:icon="@drawable/ic_home_black_24dp"
                android:title="Posted Events" />
            <item
                android:id="@+id/attending_events"
                android:icon="@drawable/ic_update_black_24dp"
                android:title="Upcoming Events" />
            <item
                android:id="@+id/settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="Settings" />
        </menu>
    </item>

    <item android:title="Sign Out">
        <menu>
            <item android:title="Sign Out"
                android:id="@+id/sign_out"
                android:icon="@drawable/ic_exit_to_app_black_24dp"/>
        </menu>
    </item>
</menu>
